const { uniqueNamesGenerator, adjectives, colors, animals, names } = require('unique-names-generator');
const faker = require('faker');
const titles = require('../titles.js');

test = faker.commerce.productAdjective();

console.log(titles())